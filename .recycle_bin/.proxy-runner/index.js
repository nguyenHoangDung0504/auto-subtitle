import { openViaProxy } from './proxyRunner.js';

// openViaProxy({
// 	targetUrl: '',
// 	wait: async (page) => await page.waitForNavigation({ waitUntil: 'domcontentloaded' }),
// 	onReady: async (page) => await page.evaluate(() => console.log('From tool:', document.querySelectorAll('img'))),
// });
